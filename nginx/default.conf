 
 #Main Nginx Reverse Proxy
 # reverse proxy config + domain mapping

server {
    listen 80;

    # Frontend
    location / {
        proxy_pass http://frontend:80;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:5000/;
    }
}